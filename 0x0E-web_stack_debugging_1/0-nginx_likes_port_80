#!/usr/bin/env bash
# Configures an Nginx server to listen on port 80.
sudo sed -i "s/8080/80/" /etc/nginx/sites-enabled/default # Create a new symbolic link pointing to the default configuration
service nginx restart # Restart the Nginx service to apply the configuration changes
