#!/usr/bin/env bash

# Check if an argument is provided
if [ $# -ne 1 ]; then
  echo "Usage: $0 <username>"
  exit 1
fi

# Switch to the specified user and run the 'whoami' command
su - "$1" -c "whoami"
