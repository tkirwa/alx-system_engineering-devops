#!/bin/bash

# Find the process IDs (PIDs) of Gunicorn workers
worker_pids=$(pgrep -f 'gunicorn.*workers')

# Gracefully reload the Gunicorn workers
for pid in $worker_pids; do
    echo "Reloading worker with PID: $pid"
    kill -HUP $pid
done

echo "Gunicorn workers reloaded successfully."
